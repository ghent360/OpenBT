# VERSION: set full version, use "devel" for development version
if [ -z "$OPENELEC_VERSION" ]; then
  OPENELEC_VERSION="devel"
fi

# OS_VERSION: OS Version
  OS_VERSION="7.0"

# ADDON_VERSION: Addon version
  ADDON_VERSION="6.0"

